name: Validar V2.4.2

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validar sintaxe Python
        run: |
          python -m py_compile app/config_safety.py app/bootstrap_v242.py app/bootstrap_v23_entry.py tests/test_config_safety.py
      - name: Executar testes de configuração
        run: python -m unittest tests/test_config_safety.py
      - name: Validar integração
        run: |
          grep -q "bootstrap_v242.install_patch" app/bootstrap_v23_entry.py
          grep -q "ConfigSafetyManager" app/bootstrap_v242.py
      - name: Validar versão publicada
        run: test "$(tr -d '\n' < VERSION)" = "2.4.2"
